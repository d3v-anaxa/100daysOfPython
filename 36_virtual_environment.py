# create v_env => $ python -m venv <dir_name>
# activate v_env => $ source <path_to_dir>/bin/activate
# deactivate v_env => $ deactivate

# create 'requirements.txt' => pip freeze > requirements.txt
# install all packages specified in 'requirements.txt' => pip instal -r <path_to_requirements.txt>